# Test Credentials

TypingArena has **no authentication**. The app is fully usable without login.

- Leaderboard scores are saved anonymously with a display name entered on the results screen.
- Personal best is stored client-side in localStorage (`ta_pb_words`, `ta_pb_time` keys).

No accounts, API keys, or passwords are required to test any flow.
